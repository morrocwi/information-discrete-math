# Benchmark / native-executor extras — NOT needed for the core `idm` solver.
#
# The core (prove_it*.py, idm, the CI `verify` job) needs only requirements.txt
# (mpmath, sympy). These packages are required only to run the native
# Retained-Contraction/Readout-Pullback prototypes and the competitor benchmarks:
#
#   benchmarks/retained_fold_tree.py                 -> numpy
#   benchmarks/compiled_retained_readout_pullback.py -> numpy, numba
#   benchmarks/retained_readout_pullback_benchmark.py-> numpy
#   external comparison column in RCP_RETAINED_READOUT_PULLBACK_STANDALONE.md
#                                                    -> opt_einsum (+ optional `autograd`)
#
# These benchmarks are prototype-tier (`finite_diagnostic`) and are NOT run in CI;
# CI exercises the pure-finite core plus the RCP energy / N-D work-token benchmarks
# (which need no third-party numerics).
#
# The Retained Spectral product has its own extras — install with
#   pip install "information-discrete-math[spectral-bench]"

numpy>=1.24
numba>=0.58
opt_einsum>=3.3
# autograd  # optional: only for the external tensor-autodiff comparison column
